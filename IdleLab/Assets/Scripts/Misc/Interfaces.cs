using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using BreakInfinity;
using static BreakInfinity.BigDouble;



/*
public interface IStationType {
    
    
}

public interface IEmployable {
    bool UnlockStatus {get; set;}
    string Type {get; set;}
    int CurrentStation {get; set;}
    BigDouble Salary {get;}
    void Initialize(int workerIndex);
    void Hire (int stationNumber);
    void Reassign(int fromStation, int toStation);
    void CalculateDurationBonus();
    void CalculateSalaryImpact();
    BigDouble DurationBonus {get; set;}
}

public interface IWorkable<T> where T : IEmployable{
    int StationNumber {get; set;}
    void Initialize(int stationNumber, GameObject station);
    void UpgradeProductionLevel();
    // void HireWorker();
    // void UpdateWorkerAssignment(T worker, int stationNumber, bool adding);
    // List<T> Workers {get; set;}
    BigDouble ProductionPerSec {get; set;}
    void SaveToData();
}
*/